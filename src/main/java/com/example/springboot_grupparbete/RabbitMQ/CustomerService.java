package com.example.springboot_grupparbete.RabbitMQ;public class CustomerService { private final RabbitTemplate rabbitTemplate;  private final Exchange exchange;  public CustomerService(RabbitTemplate rabbitTemplate, Exchange exchange) {    this.rabbitTemplate = rabbitTemplate;    this.exchange = exchange;  }  public void createCustomer() {    // ... do some database stuff    String routingKey = "Beställning.created";    String message = "Du har gjort en beställning";    rabbitTemplate.convertAndSend(exchange.getName(), routingKey, message);  }}